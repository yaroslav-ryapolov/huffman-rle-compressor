Implementation of some compression, encryption and image processing algorithms.
Algorithm list:
* Run length encoding plus huffman (EncryptionLab1).
* Hamming code (EncryptionLab2).
* RGB<->YUV conversions (MultimediaLab1_2).
* Histogram building for some component of image (MultimediaLab3).
* Discrete cosine transform (MultimediaLab4).
