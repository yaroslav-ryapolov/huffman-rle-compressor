Implementation of some compression, encryption and image processing algorithms.
Algorithm list:
* Run lenght encoding plus huffman.
* RGB<->YUV convertations.
* Histogram building for some component of image.
